name: release-gate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      full:
        description: "Run the full compatibility matrix"
        required: false
        default: false
        type: boolean
      public:
        description: "Run public benchmark and coverage suite"
        required: false
        default: false
        type: boolean
      release_candidate:
        description: "Run release-candidate gate"
        required: false
        default: false
        type: boolean
      public_runs:
        description: "Number of public benchmark timing runs"
        required: false
        default: "3"
        type: string

permissions:
  contents: read

jobs:
  release-gate:
    name: release gate
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      CI: "1"
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      FULL: ${{ github.event_name == 'workflow_dispatch' && inputs.full && '1' || '0' }}
      PUBLIC: ${{ github.event_name == 'workflow_dispatch' && inputs.public && '1' || '0' }}
      RELEASE_CANDIDATE: ${{ github.event_name == 'workflow_dispatch' && inputs.release_candidate && '1' || '0' }}
      PUBLIC_RUNS: ${{ github.event_name == 'workflow_dispatch' && inputs.public_runs || '1' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Rust
        run: |
          rustup toolchain install 1.93.0 --profile minimal
          rustup component add rustfmt --toolchain 1.93.0
          rustup component add clippy --toolchain 1.93.0
          rustup default 1.93.0

      - name: Install Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Enable pnpm through corepack
        run: corepack enable

      - name: Release gate
        run: |
          if [[ "$RELEASE_CANDIDATE" == "1" ]]; then
            FULL=1 PUBLIC=1 PUBLIC_RUNS="$PUBLIC_RUNS" scripts/release-candidate.sh
          else
            scripts/release-gate.sh
          fi
